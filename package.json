{
  "private": true,
  "scripts": {
    "start": "dotnet tool restore && dotnet fable watch src --runFast vite --host",
    "build": "dotnet tool restore && dotnet fable src --run vite build && npx vite",
    "serve": "npx vite",
    "clean": "dotnet fable clean src --yes"
  },
  "dependencies": {
    "bulma": "^1.0.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
